run:
  tmpfs:
    - "/tmp:exec,mode=1777"
  volumes:
    - "$HOME/.npm:/build/.npm:rw"
  env:
    - XDG_CACHE_HOME=/build/.cache
    - XDG_DATA_HOME=/build/.local/share
    - XDG_STATE_HOME=/build/.local/state
